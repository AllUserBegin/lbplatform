<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.erp.Mapper.UserSource.sys.SysMenuMapper">
<!-- 可根据自己的需求，是否要使用 -->
<resultMap type="com.erp.entity.sys.SysMenuBean" id="sysMenuMap">
<result property="menuId" column="menu_id"/>
<result property="parentId" column="parent_id"/>
<result property="name" column="name"/>
<result property="url" column="url"/>
<result property="perms" column="perms"/>
<result property="type" column="type"/>
<result property="icon" column="icon"/>
<result property="orderNum" column="order_num"/>
</resultMap>
    <select id="listAllByUserId" parameterType="Long"  resultType="com.erp.entity.sys.SysMenuBean" >
        SELECT  *   FROM    sys_menu   WHERE   menu_id IN ( SELECT menu_id  FROM   sys_role_menu WHERE  role_id IN ( SELECT role_id  FROM   sys_user_role  where user_id = #{user_id} ) )
    </select>
</mapper>
